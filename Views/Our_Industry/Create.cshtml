@model Primis_Technical_Solutions.Models.Our_Industry

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Our_Industry</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @* add method="post" enctype="multipart/form-data to the form tag to accept uploaded files *@
        <form asp-action="Create" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Industry_Title" class="control-label"></label>
                @*  to use the SUMMERNOTE WYSIWYG editor, put it in the textarea tag with the id of the JS script *@
                <textarea id="summernote1" asp-for="Industry_Title" class="form-control"></textarea>
                <span asp-validation-for="Industry_Title" class="text-danger"></span>
                @* the JS script of the SUMMERNOTE WYSIWYG editor *@
                <script>
                    $('#summernote1').summernote({
                        placeholder: 'Hello Bootstrap 4',
                        tabsize: 2,
                        height: 50
                    });
                </script>
            </div>
            <div class="form-group">
                <label asp-for="Indutry_Icon" class="control-label"></label>
                @* add the type="file" to the input tag to accept uploaded files *@
                <input type="file" asp-for="Indutry_Icon" class="form-control" />
                <span asp-validation-for="Indutry_Icon" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
