@model IEnumerable<Primis_Technical_Solutions.Models.Partners_Section>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<hr />
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Partner_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner_Icon)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Partner_Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Partner_Icon)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Partners_SectionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Partners_SectionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Partners_SectionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
